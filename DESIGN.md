# DESIGN.md

## 1. 데이터 분석

### metadata.json 구조 파악

metadata.json을 분석했을 때, 데이터는 크게 세 가지 계층으로 구성되어 있었습니다.

`도면 -> 공종 -> 리비전, region`

도면은 가장 기본이 되는 영역의 지도를 뜻하고 , 그 내부에 여러 공종들이 존재합니다. 공종은 각각 다른 역할을 가지고 있어서 해당 역할에 맞는 지도들을 보여주게 되고, 그 과정에서 리비전이나 region 등을 가지고 있는 경우가 있습니다.

도면의 경우는 무조건 존재하지만 , 공종이나 리비전, region 의 경우는 선택적으로 존재합니다. 또한 리비전과 region 의 경우 버전이 존재하고 이를 나눠서 보여줘야 한다는 점이 있었습니다. 따라서 가장 큰 부분에서 내려오면서 표시하였고, 그 과정에서 분기나 선택을 만나게 되면 예외 사항을 넣는 식으로 작업했습니다.

데이터의 경우는 대부분이 좌표를 표현하기 위한 데이터인데, 이미지가 등록될 때 미리 처리가 되어서 일관적인 좌표를 유지한다면 내부 계산도 사라지고 데이터의 크기도 줄어들지 않을까 하는 생각이 들었습니다.

## 2. 접근 방식

전체 과제를 Phase 단위로 나누어 점진적으로 구현했습니다.

1. 데이터 분석 및 분류 : metadata.json의 타입 정의 및 분류
2. 페이지 레이아웃 구성 및 이미지 표출
3. 도면 탐색 및 zoom 등의 기본 동작 구성, 네비게이션 설정
4. 공종별 도면 오버레이 구성, 도면을 동일 좌표에 겹치도록 설정
5. 공종별 도면 폴리곤 렌더링
6. 리비전 타임라인 구성 및 리비전별 선택 구현
7. 컴포넌트 리팩토링

각 Phase마다 PR을 생성하여 기능 단위로 이력을 관리했습니다.

---

## 3. UI 설계 결정

### 레이아웃

기본적으로 좌측 사이드바 , 상단 네비게이션 바, 우측 사이드바의 3단 레이아웃으로 구현했습니다.

- 좌측 사이드바 : 현재 표현 가능한 모든 도면 목록을 보여주고 도면별로 한 번에 이동할 수 있도록 설정
- 상단 네비게이션 바 : 브레드크럼으로 쉽게 이동할 수 있게 함. 공종별로 선택할 수 있고, 모드를 조정 가능하도록 버튼을 추가함
- 우측 사이드바 : 필요시에 등장하는 사이드바로 리비전 변경 이력이나 오버레이 컨트롤 등 필요시에 도면을 컨트롤할 수 있도록 설정

그 외에 고려했던 부분은 아래와 같습니다.

- 우측 사이드바 대신 화면에 떠있는 컨트롤 박스
  - 해당 경우 초반에 진행했지만 컨트롤에 들어가는 정보가 점점 많아지고, 그러다보니 지도를 가리게 되어서 변경하게 되었습니다. 또한 컨트롤 추가시마다 가변되는 부분도 있어서 채택하지 않았습니다.

- 좌측 사이드바 컨트롤도 함께 있는 형태
  - 지금보다 데이터가 많아지거나 할 경우에는 좌측 사이드바가 너무 길어지고, 유저가 한 눈에 컨트롤을 조정하기 힘들겠다고 판단해 변경하게 되었습니다.

## 4. 기술 선택

| 기술                     | 선택 이유                                                                        |
| ------------------------ | -------------------------------------------------------------------------------- |
| **Vite**                 | 빠른 HMR과 빌드. ssr 이 필요 없는 csr 형태의 프로젝트여서 next 대신 vite 채택    |
| **Tailwind CSS v4**      | ui 를 쉽고 빠르게 구현 가능하다는 장점                                           |
| **Zustand**              | store 와 selector 형태로 쉽게 상태 관리 가능                                     |
| **react-zoom-pan-pinch** | 도면 이미지의 줌/팬을 안정적으로 지원하는 라이브러리                             |
| **FSD 아키텍처**         | entities/features/widgets 계층으로 관심사와 사용처를 분리해서 구조적 일관성 유지 |

---

## 5. 어려웠던 점 및 해결 과정

### imageTransform / polygonTransform 좌표 변환

가장 어려웠던 부분은 서로 다른 도면과 폴리곤을 좌표계에 정렬하는 부분이었습니다. 처음에는 모두 다른 도면을 강제로 화면에 일치시키는 방향으로 진행했는데, 크기가 다르고 로테이션이 다른 지도라 일치시키기가 쉽지 않았습니다. 따라서 한 도면을 base 로 두고 다른 도면을 그에 비교시켜서 상대적인 위치를 구해서 조정하는 방식을 택했습니다.

### 비교 모드, 오버레이 모드 등 여러 모드 변경 시 상태 충돌

비교 모드에서 오버레이 모드로 전환할 때 비교 뷰어가 계속 렌더링되는 버그가 있었습니다. 오버레이 모드 진입 시 비교 모드를 해제하는 로직을 추가하여 해결했습니다.

### 시간이 더 주어진다면

- 반응형 레이아웃 구현 : 태블릿에서도 볼 수 있게 구현해 이동하면서 작업자와 소통 가능
